#include "ns3/mobility-model.h"

using namespace ns3;

int
main ()
{
  // current cartesian position, speed of an obj.
  // course change notifier

  // GetObject<MobilityModel>

  // początkowa pozycja -> PositionAllocator, używamy MobilityHelper

  // Coordinate system
}